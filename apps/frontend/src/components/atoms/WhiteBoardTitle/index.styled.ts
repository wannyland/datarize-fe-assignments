import styled from 'styled-components'

const Container = styled.div`
  display: flex;
  justify-content: space-between;
`

const Title = styled.p`
  font-size: 16px;
  font-weight: 500;
`

const Wrapper = styled.div`
  display: flex;
  align-items: center;
`

export const StyledWhiteBoardHead = {
  Title,
  Wrapper,
  Container,
}
